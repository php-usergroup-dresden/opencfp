{% extends "layouts/splash.twig" %}
{% block content %}
    <div class="opencfp-signup">
        <a class="font-serif flex items-center justify-center mb-4" href="{{ url('homepage') }}">
            <a class="flex items-center justify-center mb-4" href="{{ url('homepage') }}">
                <img class="w-1/6 mr-4" src="{{ assets('img/phpugdd-logo.svg') }}" alt="PHP USERGROUP DRESDEN e.V.">
                <img class="w-3/5" src="{{ assets('img/PHPDD18-Logo.svg') }}" alt="PHP Developer Days 2018">
            </a>
        </a>

        {% include "_flash.twig" %}

        <form method="post" action="{{ url('user_create') }}">
            <label class="text-white text-sm">Email</label>
            <input type="email" name="email" placeholder="you@example.com">
            <label class="text-white text-sm">Password</label>
            <input type="password" name="password" placeholder="•••••••">
            <div class="text-sm my-4 flex items-center">
                <input id="coc" class="mr-2" name="coc" type="checkbox">
                <label for="coc" class="text-muted">I agree to abide by the <a href="{{ site.coc_link }}" target="_blank" class="text-brand text-bold text-underline" rel="noopener noreferrer">Code of Conduct</a></label>
            </div>
            <button type="submit" class="btn btn-brand w-full">Signup</button>
        </form>
    </div>
{% endblock %}
