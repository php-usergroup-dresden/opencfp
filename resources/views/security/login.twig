{% extends "layouts/splash.twig" %}
{% block content %}
  <div class="opencfp-login">
    <a class="flex items-center justify-center mb-4" href="{{ url('homepage') }}">
      <img class="w-1/6 mr-4" src="{{ assets('img/phpugdd-logo.svg') }}" alt="PHP USERGROUP DRESDEN e.V.">
      <img class="w-3/5" src="{{ assets('img/PHPDD18-Logo.svg') }}" alt="PHP Developer Days 2018">
    </a>

    {% include "_flash.twig" %}

    <form method="post" action="{{ url('login_check') }}">
      <label for="email" class="text-sm text-white">Email</label>
      <input id="email" type="email" name="email" value="{% if email %}{{ email | escape }}{% endif %}" class="w-full" placeholder="you@example.com">
      <label for="password" class="text-sm text-white">Password</label>
      <input id="password" type="password" name="password" class="w-full" placeholder="•••••••">
      <p class="text-right my-4"><a href="{{ url('forgot_password') }}" class="text-white text-sm">Forgot password?</a></p>
      <button type="submit" class="btn btn-brand w-full">Login</button>
    </form>

    <p class="text-muted text-center text-sm my-4">New user? <a href="{{ url('user_new') }}" class="text-brand text-bold">Signup Now</a></p>
  </div>
{% endblock %}
